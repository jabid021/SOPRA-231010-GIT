import { MonFiltrePipe } from './mon-filtre.pipe';

describe('MonFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new MonFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
